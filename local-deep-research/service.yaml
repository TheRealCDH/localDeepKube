apiVersion: v1
kind: Service
metadata:
  name: local-deep-research
  labels:
    app.kubernetes.io/name: local-deep-research
spec:
  selector:
    app.kubernetes.io/name: local-deep-research
  ports:
    - port: 5000
      targetPort: 5000
      name: http
  type: ClusterIP