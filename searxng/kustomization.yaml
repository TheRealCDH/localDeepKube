apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: searxng-config
    files:
      - settings.yml

labels:
  - pairs:
      app.kubernetes.io/part-of: localdeepkube